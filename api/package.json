{
  "name": "api",
  "version": "1.0.0",
  "description": "Guess Api",
  "main": "src/server.ts",
  "scripts": {
    "dev": "nodemon src/server.ts",
    "prettier": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "prettier-check": "prettier --config .prettierrc 'src/**/*.ts' --list-different",
    "build": "rm -rf dest/ && prettier --write 'src/**/*.ts' && tsc",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "keywords": [],
  "author": "Jakob Jónasson",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/morgan": "^1.9.3",
    "express": "^4.17.2",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/supertest": "^2.0.11",
    "jest": "^27.4.5",
    "prettier": "^2.5.1",
    "supertest": "^6.1.6",
    "ts-jest": "^27.1.2"
  }
}
